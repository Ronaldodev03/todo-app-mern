export const toast = {
  success: (message) => {
    console.log('SUCCESS:', message);
  },
  error: (message) => {
    console.error('ERROR:', message);
  },
  info: (message) => {
    console.info('INFO:', message);
  },
};
